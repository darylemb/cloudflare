# Local Terraform state
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
*.tfstate.backup

# backend.tfvars is generated at runtime in CI from secrets; never commit
backend.tfvars

# OCI API key (created by CI)
oci_api_key.pem

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
